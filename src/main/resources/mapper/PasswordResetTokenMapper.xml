<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.apt.mail.mapper.PasswordResetTokenMapper">

    <!-- 토큰 저장 -->
    <insert id="saveToken">
        INSERT INTO password_reset_token (user_id, token, expired_at, created_at)
        VALUES (#{userId}, #{token}, #{expiredAt}, NOW())
    </insert>

    <!-- 토큰으로 조회 -->
    <select id="findByToken" resultType="com.apt.mail.model.PasswordResetToken">
        SELECT token_id, user_id, token, expired_at, created_at
        FROM password_reset_token
        WHERE token = #{token}
    </select>

    <!-- 사용자 ID로 기존 토큰 삭제 -->
    <delete id="deleteByUserId">
        DELETE
        FROM password_reset_token
        WHERE user_id = #{userId}
    </delete>

</mapper>