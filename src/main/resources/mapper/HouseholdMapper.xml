<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.apt.household.mapper.HouseholdMap">

    <select id="findByDongAndHo" resultType="com.apt.household.dto.Household">
        SELECT household_id, dong, ho, created_at
        FROM household
        WHERE dong = #{dong} AND ho = #{ho}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="householdId"
            parameterType="com.apt.household.dto.Household">
        INSERT INTO household (dong, ho, created_at)
        VALUES (#{dong}, #{ho}, NOW())
    </insert>

    <select id="findAll" resultType="com.apt.household.dto.Household">
        SELECT household_id, dong, ho, created_at
        FROM household
        ORDER BY dong, ho
    </select>

    <select id="findById" resultType="com.apt.household.dto.Household">
        SELECT household_id, dong, ho, created_at
        FROM household
        WHERE household_id = #{householdId}
    </select>

    <update id="update" parameterType="com.apt.household.dto.Household">
        UPDATE household
        SET dong = #{dong}, ho = #{ho}
        WHERE household_id = #{householdId}
    </update>
</mapper>