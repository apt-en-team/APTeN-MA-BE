<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.apt.reservation.mapper.ReservationMapper">

    <!-- 상태 업데이트  -->
    <update id="updateStatusToCompleted">
        UPDATE reservation
        SET status = CASE
             WHEN status = 'CONFIRMED' THEN 'COMPLETED'
             WHEN status = 'PENDING'   THEN 'CANCELLED'
        END
        WHERE status IN ('CONFIRMED', 'PENDING')
        AND reservation_date &lt; #{today}
    </update>
    <!-- 예약 가능 시간대 조회 -->
    <select id="findAvailableTime"
            parameterType="com.apt.reservation.dto.request.ReservationReq"
            resultType="com.apt.reservation.dto.response.AvailableSlotRes">
        SELECT
            start_time  AS startTime,
            COUNT(*)    AS reservedCount
        FROM reservation
        WHERE facility_id = #{facilityId}
          AND reservation_date = #{reservationDate}
          AND status = 'CONFIRMED'
        GROUP BY start_time
        ORDER BY start_time
    </select>

    <!-- 좌석 중복 체크 -->
    <select id="countSeatReserved">
        SELECT COUNT(*) FROM reservation
        WHERE facility_id = #{facilityId}
          AND reservation_date = #{reservationDate}
          AND start_time = #{startTime}
          AND seat_no = #{seatNo}
          AND status = 'CONFIRMED'
        FOR UPDATE
    </select>
    <!-- 정원 초과 체크 -->
    <select id="countReserved">
        SELECT COUNT(*) FROM reservation
        WHERE facility_id = #{facilityId}
          AND reservation_date = #{reservationDate}
          AND start_time = #{startTime}
          AND status = 'CONFIRMED'
    </select>
    <!-- 예약 생성 -->
    <insert id="insertReservation"
            parameterType="com.apt.reservation.dto.request.ReservationReq"
            useGeneratedKeys="true"
            keyProperty="reservationId">
        INSERT INTO reservation
            SET user_id = #{userId}
            , facility_id = #{facilityId}
            , reservation_date = #{reservationDate}
            ,start_time = #{startTime}
            , end_time = #{endTime}
            , seat_no =  #{seatNo}
            , program_id = #{programId}
            , status = #{status}
    </insert>

    <!-- 예약 상세 조회 -->
    <select id="findReservationById"
            resultType="com.apt.reservation.dto.response.ReservationRes">
        SELECT
            r.reservation_id,
            r.user_id,
            r.facility_id,
            f.name AS facilityName,
            r.reservation_date,
            r.start_time,
            r.end_time,
            r.seat_no,
            r.status,
            r.created_at,
            (SELECT COUNT(*)
             FROM reservation r2
             WHERE r2.facility_id      = r.facility_id
               AND r2.reservation_date = r.reservation_date
               AND r2.start_time       = r.start_time
               AND r2.status           = 'CONFIRMED') AS currentCount,
            (SELECT COUNT(*)
             FROM reservation r2
             WHERE r2.program_id = r.program_id
               AND r2.status     = 'PENDING'
               AND r.program_id IS NOT NULL) AS pendingCount,
            gx.start_date,
            gx.end_date,
            gx.program_id,
            u.name as userName,
            h.dong,
            h.ho,
            r.approved_at,
            r.cancelled_at,
            f.max_capacity
        FROM reservation r
                 JOIN facility f ON f.facility_id = r.facility_id
                 LEFT JOIN gx_program gx ON gx.program_id = r.program_id
                 LEFT JOIN user u ON u.user_id = r.user_id
                 LEFT JOIN household h ON u.household_id = h.household_id

        WHERE r.reservation_id = #{reservationId}
    </select>

    <!-- 전체 건수 조회 (페이지네이션용) -->
    <select id="countAllByAdmin" resultType="int">
        SELECT COUNT(*)
        FROM reservation r
        JOIN facility f ON r.facility_id = f.facility_id
        JOIN user u ON r.user_id = u.user_id
        LEFT JOIN household h ON h.household_id = u.household_id
        <where>
            <if test="status != null and status != ''">
                AND r.status = #{status}
            </if>
            <if test="facilityName != null and facilityName != ''">
                AND f.name LIKE CONCAT('%', #{facilityName}, '%')
            </if>
            <if test="facilityId != null">
                AND r.facility_id = #{facilityId}
            </if>
            <if test="reservationDate != null">
                AND r.reservation_date = #{reservationDate}
            </if>
            <if test="userName != null and userName != ''">
                AND u.name LIKE CONCAT('%', #{userName}, '%')
            </if>
        </where>
    </select>

    <!-- 내 예약 조회 - 이용예정/지난내역/시설이름 필터 -->
    <select id="findAll" resultType="com.apt.reservation.dto.response.ReservationRes">
        SELECT r.reservation_id,
               r.user_id,
               f.name as facilityName,
               r.reservation_date,
               r.start_time,
               r.end_time,
               r.seat_no,
               r.status

        FROM reservation r
        left join facility f
        ON r.facility_id = f.facility_id
        WHERE r.user_id = #{userId}
        <if test='status == "CONFIRMED"'>
            AND r.status IN ('CONFIRMED', 'PENDING')
        </if>
        <if test='status == "COMPLETED"'>
            AND r.status IN ('COMPLETED', 'CANCELLED')
        </if>
        <if test="facilityName != null and facilityName != ''">
            AND f.name LIKE CONCAT('%', #{facilityName}, '%')
        </if>
        ORDER BY r.reservation_date DESC, r.start_time DESC
        LIMIT #{startIdx}, #{size}
    </select>

    <!-- 예약 취소 -->
    <update id="cancelReservation">
        UPDATE reservation
        SET seat_no = NULL,
            status   = 'CANCELLED',
            cancelled_at = NOW()
        WHERE reservation_id = #{id}
    </update>

    <!-- 예약 전체 취소 -->
    <update id="cancelAllReservation">
        UPDATE reservation
        SET seat_no = NULL,
            status   = 'CANCELLED',
            cancelled_at = NOW()
        WHERE facility_id = #{facilityId}
        AND status in('CONFIRMED','PENDING')
    </update>

    <!-- 관리자 전체예약 조회 -->
    <select id="findAllByAdmin" resultType="com.apt.reservation.dto.response.ReservationListRes">
        SELECT
        r.reservation_id, r.reservation_date, r.start_time, r.end_time,
        r.seat_no, r.status, r.created_at, r.cancelled_at, r.program_id, r.approved_at,
        f.facility_id, f.name AS facilityName, f.max_capacity,
        u.user_id, u.name AS userName,
        h.dong, h.ho,
        (SELECT COUNT(*)
        FROM reservation r2
        WHERE r2.facility_id      = r.facility_id
        AND r2.reservation_date = r.reservation_date
        AND r2.start_time       = r.start_time
        AND r2.status           = 'CONFIRMED') AS currentCount,
        (SELECT COUNT(*)
        FROM reservation r2
        WHERE r2.program_id = r.program_id
        AND r2.status     = 'PENDING'
        AND r.program_id IS NOT NULL) AS pendingCount
        FROM reservation r
        JOIN facility f ON r.facility_id = f.facility_id
        JOIN user u ON r.user_id = u.user_id
        LEFT JOIN household h ON h.household_id = u.household_id
        <where>
            <if test="status != null and status != ''">
                AND r.status = #{status}
            </if>
            <if test="facilityName != null and facilityName != ''">
                AND f.name LIKE CONCAT('%', #{facilityName}, '%')
            </if>
            <if test="facilityId != null">
                AND r.facility_id = #{facilityId}
            </if>
            <if test="reservationDate != null">
                AND r.reservation_date = #{reservationDate}
            </if>
            <if test="userName != null and userName != ''">
                AND u.name LIKE CONCAT('%', #{userName}, '%')
            </if>
        </where>
        ORDER BY r.reservation_date DESC, r.start_time DESC
        LIMIT #{startIdx}, #{size}
    </select>

    <!-- Gx프로그램 조회 model 셋팅-->
    <select id="findProgramById">
        SELECT gx.program_id, gx.start_date, gx.end_date, gx.days_of_week, gx.status, gx.created_at,
               f.facility_id, f.max_capacity
        FROM gx_program gx
        JOIN facility f
        ON gx.facility_id = f.facility_id
        WHERE gx.program_id = #{programId}
    </select>

    <!-- 대기목록 조회(선착순 정렬) -->
    <select id="findPendingByProgramId">
        SELECT r.reservation_id, r.user_id, r.program_id, r.start_time, r.end_time, r.seat_no,r.status, r.created_at,
               f.facility_id, f.name as facilityName

        FROM reservation r
        JOIN facility f
        ON r.facility_id = f.facility_id
        WHERE r.program_id = #{programId}
        AND r.status = 'PENDING'
        ORDER BY r.created_at
    </select>

    <!-- 대기 -> 확정 처리 -->
    <update id="approveReservation">
        UPDATE reservation
        SET status   = 'CONFIRMED',
            approved_at = NOW()
        WHERE reservation_id = #{reservationId}
    </update>

    <!-- 대기 -> 취소 처리 (정원초과 분) -->
    <update id="cancelOverflowReservation">
        UPDATE reservation
        SET status   = 'CANCELLED',
            cancelled_at = NOW()
        WHERE reservation_id = #{reservationId}
    </update>

    <!-- 승인인원 = 맥스인원 이면 프로그램 CLOSED -->
    <update id="closeProgram">
        UPDATE gx_program
        SET status   = 'CLOSED',
            updated_at = NOW()
        WHERE program_id = #{programId}
    </update>

    <select id="pendingGx" resultType="com.apt.reservation.dto.response.GxPendingCount">
        SELECT count(*) AS pendingCount
        FROM reservation
        WHERE status = 'PENDING'
    </select>

    <select id="TodayStats" resultType="com.apt.reservation.dto.response.TodayStatsRes">
        SELECT
        COUNT(CASE WHEN reservation_date = CURDATE() THEN 1 END) AS todayTotal,
        COUNT(CASE WHEN reservation_date = CURDATE()
                    AND NOW() BETWEEN start_time AND end_time
                    AND status = 'CONFIRMED'
                    THEN 1 END) AS todayConfirmed,
        COUNT(CASE WHEN reservation_date = CURDATE()
                    AND status = 'CANCELLED'
                    THEN 1 END) AS todayCancelled,
        COUNT(CASE
                  WHEN reservation_date = CURDATE()
                      AND status = 'PENDING'
                      THEN 1 END) AS todayPending,
        COUNT(CASE WHEN DATE_FORMAT(reservation_date, '%Y-%m') = DATE_FORMAT(CURDATE(), '%Y-%m')
                          THEN 1 END) AS monthTotal,
        COUNT(*) AS totalCount
        FROM reservation
    </select>


    <!-- 관리자 달력페이지 조회 -->
    <select id="getReservationsByFacility">
        SELECT r.reservation_id, f.facility_id, f.name as facilityName, ft.type_id, ft.name as typeName, r.reservation_date,
               r.start_time, r.end_time, r.seat_no, r.status, u.name as userName, h.dong, h.ho, gx.program_id, f.max_capacity
        FROM reservation r
                 LEFT JOIN facility f
                           ON r.facility_id = f.facility_id
                 LEFT JOIN facility_type ft
                           ON f.type_id = ft.type_id
                 LEFT JOIN gx_program gx
                           ON r.program_id = gx.program_id
                 LEFT JOIN user u
                           ON r.user_id = u.user_id
                 LEFT JOIN household h
                           ON u.household_id = h.household_id
        WHERE ft.type_id = #{typeId}
          AND r.reservation_date between #{startDate} and #{endDate};
    </select>


</mapper>